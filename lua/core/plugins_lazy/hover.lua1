-- doesn'T really work anyway
return {
	"lewis6991/hover.nvim",
	config = function()
		require("hover").setup({
			init = function() require("hover.providers.lsp") end,
			preview_opts = {
				border = "rounded",
				max_width = 80,
				max_height = 25,
				focusable = true,
			},
			title = true,
		})
		vim.keymap.set("n", "K", require("hover").hover, { silent = true })
	end,
}
